import { FormTextField } from '@design-system'
import { styled, Box } from '@mui/material'

export const CreateChatTextField = styled(FormTextField)({
  marginBottom: '8px',
})

export const CreateChatButtons = styled(Box)({
  marginTop: '16px',
  display: 'flex',
  alignItems: 'center',
})
