import { styled, Box, Card } from '@mui/material'

export const ChatListPageBox = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  padding: '8px',
})

export const ChatListPageCard = styled(Card)({
  margin: '10px',
  width: '100%',
  cursor: 'pointer',
})
