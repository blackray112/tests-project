import { Accordion, styled } from '@mui/material'
import { ArrowDownCircle } from 'react-feather'

export const StyledAccordion = styled(Accordion)({
  width: '100%',
})

export const StyledExpandMore = styled(ArrowDownCircle)({
  margin: '0 10px',
})
